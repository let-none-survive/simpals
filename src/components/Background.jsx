import styled from 'styled-components';

const Background = styled.div`
  background: #e3e3e3;
  color: #000;
  min-height: 100%;
  overflow: hidden;
  position: relative;
`;

export default Background;
